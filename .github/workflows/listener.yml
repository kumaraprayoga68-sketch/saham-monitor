name: Listener Telegram

on:
  schedule:
    # Tiap 5 menit (interval tercepat GitHub). Balasan command bisa delay s/d 5 menit.
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: listener-telegram
  cancel-in-progress: false

jobs:
  listen:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kode
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependency
        run: pip install -r requirements.txt

      - name: Proses pesan masuk
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python listener.py

      - name: Simpan offset (commit balik kalau berubah)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ -n "$(git status --porcelain tg_offset.json)" ]; then
            git add tg_offset.json
            git commit -m "chore: update offset telegram [skip ci]"
            git pull --rebase --autostash origin main
            git push
          else
            echo "Offset tidak berubah, tidak commit."
          fi
